# Optional — only if the API is NOT proxied in dev (see vite.config.js).
# In normal dev, leave unset so requests go to /api/transcribe on the Vite origin.
# VITE_TRANSCRIBE_API_URL=http://localhost:8787
